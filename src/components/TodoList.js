import React, {Component} from 'react';

//  import PropTypes from 'prop-types';

class TodoList extends Component {
    render() {
        return (
            <div>
                This is wrapper for todo List
            </div>
        );
    }
}

//  TodoList.propTypes = {};

export default TodoList;
